/**
 * Enum of available top-level commands
 *
 * @type CommandEnum
 */
export enum CommandEnum {
    Init = 'init',
    Entry = 'entry',
    Compile = 'compile',
    Generate = 'generate',
    Help = 'help',
}
